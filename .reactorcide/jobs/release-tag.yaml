name: ichoi-release-tag
description: "Create release tags and stamp version files after a merge."
job:
  image: containers.catalystsquad.com/public/reactorcide/runnerbase:latest
  command: "runnerlib run --job-command true"
  timeout: 1800
  priority: 15
  working_dir: /job
  disable_run_local: true
  run_as:
    user: runner
environment:
  ICHOI_RELEASE_JOB: tag
  ICHOI_REPOSITORY: catalystcommunity/ichoi
  GITHUB_PAT: "${secret:catalystcommunity/ci:githubpat}"
