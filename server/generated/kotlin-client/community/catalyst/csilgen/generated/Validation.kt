// Generated validation functions.
// Code generated by csilgen; DO NOT EDIT.

package community.catalyst.csilgen.generated

/** Validate a SearchRequest; throws IllegalArgumentException on a constraint breach. */
fun SearchRequest.validate() {
    require(query.length >= 1) { "field 'query' must have at least 1 characters" }
    require(query.length <= 256) { "field 'query' must have at most 256 characters" }
}

/** Validate a PlayerState; throws IllegalArgumentException on a constraint breach. */
fun PlayerState.validate() {
    require(volume <= 100uL) { "field 'volume' must be at most 100" }
}

/** Validate a CmdVolume; throws IllegalArgumentException on a constraint breach. */
fun CmdVolume.validate() {
    require(volume <= 100uL) { "field 'volume' must be at most 100" }
}

/** Validate a EnableShareRequest; throws IllegalArgumentException on a constraint breach. */
fun EnableShareRequest.validate() {
    suffix?.let { v ->
        require(v.length >= 1) { "field 'suffix' must have at least 1 characters" }
    }
    suffix?.let { v ->
        require(v.length <= 48) { "field 'suffix' must have at most 48 characters" }
    }
}

/** Validate a DirVolume; throws IllegalArgumentException on a constraint breach. */
fun DirVolume.validate() {
    require(volume <= 100uL) { "field 'volume' must be at most 100" }
}

/** Validate a RenameNodeRequest; throws IllegalArgumentException on a constraint breach. */
fun RenameNodeRequest.validate() {
    require(friendlyName.length >= 1) { "field 'friendlyName' must have at least 1 characters" }
    require(friendlyName.length <= 64) { "field 'friendlyName' must have at most 64 characters" }
}

/** Validate a RenameDeviceRequest; throws IllegalArgumentException on a constraint breach. */
fun RenameDeviceRequest.validate() {
    require(friendlyName.length >= 1) { "field 'friendlyName' must have at least 1 characters" }
    require(friendlyName.length <= 64) { "field 'friendlyName' must have at most 64 characters" }
}

/** Validate a CreateGroupRequest; throws IllegalArgumentException on a constraint breach. */
fun CreateGroupRequest.validate() {
    require(name.length >= 1) { "field 'name' must have at least 1 characters" }
    require(name.length <= 64) { "field 'name' must have at most 64 characters" }
}

