/* Generated CSIL value types. */
/* Code generated by csilgen; DO NOT EDIT. */
#ifndef CSILGEN_TYPES_GEN_H
#define CSILGEN_TYPES_GEN_H

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include "csil_timestamp.gen.h"

typedef struct CsilBytes {
    uint8_t *data;
    size_t len;
} CsilBytes;

/* Role is an enumeration. */
typedef enum Role {
    ROLE_ADMIN,
    ROLE_MEMBER,
    ROLE_GUEST,
} Role;

/* PlayerStatus is an enumeration. */
typedef enum PlayerStatus {
    PLAYER_STATUS_STOPPED,
    PLAYER_STATUS_PLAYING,
    PLAYER_STATUS_PAUSED,
} PlayerStatus;

/* Codec is an enumeration. */
typedef enum Codec {
    CODEC_MP3,
    CODEC_AAC,
    CODEC_VORBIS,
    CODEC_FLAC,
    CODEC_ALAC,
    CODEC_OPUS,
    CODEC_WAV,
    CODEC_WMA,
} Codec;

/* TranscodeCodec is an enumeration. */
typedef enum TranscodeCodec {
    TRANSCODE_CODEC_AAC,
    TRANSCODE_CODEC_MP3,
} TranscodeCodec;

/* Library is an enumeration. */
typedef enum Library {
    LIBRARY_MUSIC,
    LIBRARY_AUDIOBOOK,
} Library;

/* PlayerKind is an enumeration. */
typedef enum PlayerKind {
    PLAYER_KIND_SHARED,
    PLAYER_KIND_PRIVATE,
} PlayerKind;

/* MediaEndReason is an enumeration. */
typedef enum MediaEndReason {
    MEDIA_END_REASON_EOS,
    MEDIA_END_REASON_STOPPED,
} MediaEndReason;

/* NodeKind is an enumeration. */
typedef enum NodeKind {
    NODE_KIND_CORE,
    NODE_KIND_SATELLITE,
    NODE_KIND_CLIENT,
} NodeKind;

/* AudioOutputsState is an enumeration. */
typedef enum AudioOutputsState {
    AUDIO_OUTPUTS_STATE_NONE,
    AUDIO_OUTPUTS_STATE_SOME,
} AudioOutputsState;

/* Forward declarations (resolve mutual and out-of-order references). */
typedef struct StreamPref StreamPref;
typedef struct Page Page;
typedef struct Ok Ok;
typedef struct ServiceError ServiceError;
typedef struct AuthRequest AuthRequest;
typedef struct SessionInfo SessionInfo;
typedef struct Track Track;
typedef struct Album Album;
typedef struct Artist Artist;
typedef struct Playlist Playlist;
typedef struct BrowseRequest BrowseRequest;
typedef struct LibraryInfo LibraryInfo;
typedef struct LibrariesResponse LibrariesResponse;
typedef struct AlbumsResponse AlbumsResponse;
typedef struct ArtistsResponse ArtistsResponse;
typedef struct AlbumRequest AlbumRequest;
typedef struct AlbumDetail AlbumDetail;
typedef struct ArtistRequest ArtistRequest;
typedef struct ArtistDetail ArtistDetail;
typedef struct SearchRequest SearchRequest;
typedef struct SearchResponse SearchResponse;
typedef struct AudiobookProgress AudiobookProgress;
typedef struct AudiobookProgressRequest AudiobookProgressRequest;
typedef struct AudiobookProgressResponse AudiobookProgressResponse;
typedef struct UpdateAudiobookProgressRequest UpdateAudiobookProgressRequest;
typedef struct PlaylistsResponse PlaylistsResponse;
typedef struct PlaylistRequest PlaylistRequest;
typedef struct PlaylistDetail PlaylistDetail;
typedef struct CoverArtRequest CoverArtRequest;
typedef struct CoverArt CoverArt;
typedef struct Player Player;
typedef struct QueueItem QueueItem;
typedef struct PlayerState PlayerState;
typedef struct ListPlayersRequest ListPlayersRequest;
typedef struct ListPlayersResponse ListPlayersResponse;
typedef struct SubscribeRequest SubscribeRequest;
typedef struct CmdEnqueue CmdEnqueue;
typedef struct CmdRemove CmdRemove;
typedef struct CmdReorder CmdReorder;
typedef struct CmdClear CmdClear;
typedef struct CmdPlay CmdPlay;
typedef struct CmdPause CmdPause;
typedef struct CmdNext CmdNext;
typedef struct CmdPrevious CmdPrevious;
typedef struct CmdSeek CmdSeek;
typedef struct CmdVolume CmdVolume;
typedef struct PlayerCommand PlayerCommand;
typedef struct CommandRequest CommandRequest;
typedef struct EnableShareRequest EnableShareRequest;
typedef struct DisableShareRequest DisableShareRequest;
typedef struct ShareResult ShareResult;
typedef struct MediaOpen MediaOpen;
typedef struct MediaSeek MediaSeek;
typedef struct MediaPause MediaPause;
typedef struct MediaResume MediaResume;
typedef struct MediaStop MediaStop;
typedef struct MediaControl MediaControl;
typedef struct MediaHeader MediaHeader;
typedef struct MediaChunk MediaChunk;
typedef struct MediaEnd MediaEnd;
typedef struct MediaFail MediaFail;
typedef struct MediaEvent MediaEvent;
typedef struct AudioOutput AudioOutput;
typedef struct RegisterNodeRequest RegisterNodeRequest;
typedef struct RegisterNodeResponse RegisterNodeResponse;
typedef struct DirLoad DirLoad;
typedef struct DirPause DirPause;
typedef struct DirResume DirResume;
typedef struct DirStop DirStop;
typedef struct DirVolume DirVolume;
typedef struct NodeDirective NodeDirective;
typedef struct NodeReport NodeReport;
typedef struct Account Account;
typedef struct ListAccountsResponse ListAccountsResponse;
typedef struct SetRoleRequest SetRoleRequest;
typedef struct TrustDomainRequest TrustDomainRequest;
typedef struct TrustedDomains TrustedDomains;
typedef struct DeviceInfo DeviceInfo;
typedef struct NodeInfo NodeInfo;
typedef struct ListNodesResponse ListNodesResponse;
typedef struct RenameNodeRequest RenameNodeRequest;
typedef struct RenameDeviceRequest RenameDeviceRequest;
typedef struct CreateNodeTokenRequest CreateNodeTokenRequest;
typedef struct NodeTokenResult NodeTokenResult;
typedef struct ImportTrackRequest ImportTrackRequest;
typedef struct ImportResult ImportResult;
typedef struct Settings Settings;
typedef struct SetSettingRequest SetSettingRequest;

/* AccountId is a type alias. */
typedef char *AccountId;

/* Handle is a type alias. */
typedef char *Handle;

/* TrackId is a type alias. */
typedef char *TrackId;

/* AlbumId is a type alias. */
typedef char *AlbumId;

/* ArtistId is a type alias. */
typedef char *ArtistId;

/* PlaylistId is a type alias. */
typedef char *PlaylistId;

/* NodeId is a type alias. */
typedef char *NodeId;

/* DeviceId is a type alias. */
typedef char *DeviceId;

/* PlayerId is a type alias. */
typedef char *PlayerId;

/* StreamPref is a structured data type. */
typedef struct StreamPref {
    uint64_t *max_bitrate_kbps;
    bool *prefer_original;
    TranscodeCodec *transcode_codec;
} StreamPref;

/* Page is a structured data type. */
typedef struct Page {
    uint64_t *offset;
    uint64_t *limit;
} Page;

/* Ok is a structured data type. */
typedef struct Ok {
    bool ok;
} Ok;

/* ServiceError is a structured data type. */
typedef struct ServiceError {
    int64_t code;
    char *message;
} ServiceError;

/* AuthRequest is a structured data type. */
typedef struct AuthRequest {
    CsilBytes *linkkeys_assertion;
    char *linkkeys_exchange_code;
    char *bootstrap_token;
} AuthRequest;

/* SessionInfo is a structured data type. */
typedef struct SessionInfo {
    AccountId account_id;
    Handle handle;
    char *display_name;
    Role role;
    char *token;
} SessionInfo;

/* Track is a structured data type. */
typedef struct Track {
    TrackId id;
    Library library;
    char *title;
    ArtistId *artist_id;
    AlbumId *album_id;
    uint64_t *track_no;
    uint64_t *disc_no;
    uint64_t duration_ms;
    Codec codec;
    uint64_t *bitrate_kbps;
    uint64_t sample_rate;
    uint64_t channels;
    uint64_t *bit_depth;
    char *root_relative_path;
    char *content_hash;
} Track;

/* Album is a structured data type. */
typedef struct Album {
    AlbumId id;
    char *title;
    ArtistId *artist_id;
    uint64_t *year;
    bool has_cover_art;
    uint64_t track_count;
} Album;

/* Artist is a structured data type. */
typedef struct Artist {
    ArtistId id;
    char *name;
    uint64_t album_count;
} Artist;

/* Playlist is a structured data type. */
typedef struct Playlist {
    PlaylistId id;
    char *name;
    AccountId *owner;
    uint64_t entry_count;
    char *root_relative_path;
} Playlist;

/* BrowseRequest is a structured data type. */
typedef struct BrowseRequest {
    Library *library;
    uint64_t *offset;
    uint64_t *limit;
} BrowseRequest;

/* LibraryInfo is a structured data type. */
typedef struct LibraryInfo {
    Library kind;
} LibraryInfo;

/* LibrariesResponse is a structured data type. */
typedef struct LibrariesResponse {
    LibraryInfo *libraries;
    size_t libraries_count;
} LibrariesResponse;

/* AlbumsResponse is a structured data type. */
typedef struct AlbumsResponse {
    Album *albums;
    size_t albums_count;
    uint64_t total;
} AlbumsResponse;

/* ArtistsResponse is a structured data type. */
typedef struct ArtistsResponse {
    Artist *artists;
    size_t artists_count;
    uint64_t total;
} ArtistsResponse;

/* AlbumRequest is a structured data type. */
typedef struct AlbumRequest {
    AlbumId album_id;
} AlbumRequest;

/* AlbumDetail is a structured data type. */
typedef struct AlbumDetail {
    Album album;
    Track *tracks;
    size_t tracks_count;
} AlbumDetail;

/* ArtistRequest is a structured data type. */
typedef struct ArtistRequest {
    ArtistId artist_id;
} ArtistRequest;

/* ArtistDetail is a structured data type. */
typedef struct ArtistDetail {
    Artist artist;
    Album *albums;
    size_t albums_count;
} ArtistDetail;

/* SearchRequest is a structured data type. */
typedef struct SearchRequest {
    char *query;
    Library *library;
    uint64_t *limit;
} SearchRequest;

/* SearchResponse is a structured data type. */
typedef struct SearchResponse {
    Artist *artists;
    size_t artists_count;
    Album *albums;
    size_t albums_count;
    Track *tracks;
    size_t tracks_count;
} SearchResponse;

/* AudiobookProgress is a structured data type. */
typedef struct AudiobookProgress {
    TrackId track_id;
    uint64_t position_ms;
    bool completed;
    CsilTimestamp updated_at;
} AudiobookProgress;

/* AudiobookProgressRequest is a structured data type. */
typedef struct AudiobookProgressRequest {
    TrackId *track_ids;
    size_t track_ids_count;
} AudiobookProgressRequest;

/* AudiobookProgressResponse is a structured data type. */
typedef struct AudiobookProgressResponse {
    AudiobookProgress *progress;
    size_t progress_count;
} AudiobookProgressResponse;

/* UpdateAudiobookProgressRequest is a structured data type. */
typedef struct UpdateAudiobookProgressRequest {
    TrackId track_id;
    uint64_t position_ms;
    bool completed;
} UpdateAudiobookProgressRequest;

/* PlaylistsResponse is a structured data type. */
typedef struct PlaylistsResponse {
    Playlist *playlists;
    size_t playlists_count;
} PlaylistsResponse;

/* PlaylistRequest is a structured data type. */
typedef struct PlaylistRequest {
    PlaylistId playlist_id;
} PlaylistRequest;

/* PlaylistDetail is a structured data type. */
typedef struct PlaylistDetail {
    Playlist playlist;
    Track *tracks;
    size_t tracks_count;
} PlaylistDetail;

/* CoverArtRequest is a structured data type. */
typedef struct CoverArtRequest {
    AlbumId album_id;
    uint64_t *max_size;
} CoverArtRequest;

/* CoverArt is a structured data type. */
typedef struct CoverArt {
    char *content_type;
    CsilBytes data;
} CoverArt;

/* Player is a structured data type. */
typedef struct Player {
    PlayerId id;
    PlayerKind kind;
    char *name;
    NodeId *node_id;
    DeviceId *device_id;
    AccountId *owner;
} Player;

/* QueueItem is a structured data type. */
typedef struct QueueItem {
    TrackId track_id;
    Library *library;
    char *title;
    char *artist;
    uint64_t *duration_ms;
} QueueItem;

/* PlayerState is a structured data type. */
typedef struct PlayerState {
    PlayerId player_id;
    PlayerStatus status;
    uint64_t *current_index;
    uint64_t *position_ms;
    uint64_t volume;
    QueueItem *queue;
    size_t queue_count;
} PlayerState;

/* ListPlayersRequest is a structured data type. */
typedef struct ListPlayersRequest {
    PlayerKind *kind;
} ListPlayersRequest;

/* ListPlayersResponse is a structured data type. */
typedef struct ListPlayersResponse {
    Player *players;
    size_t players_count;
} ListPlayersResponse;

/* SubscribeRequest is a structured data type. */
typedef struct SubscribeRequest {
    PlayerId player_id;
} SubscribeRequest;

/* CmdEnqueue is a structured data type. */
typedef struct CmdEnqueue {
    char *op;
    TrackId *track_ids;
    size_t track_ids_count;
    uint64_t *at_index;
} CmdEnqueue;

/* CmdRemove is a structured data type. */
typedef struct CmdRemove {
    char *op;
    uint64_t index;
} CmdRemove;

/* CmdReorder is a structured data type. */
typedef struct CmdReorder {
    char *op;
    uint64_t from_index;
    uint64_t to_index;
} CmdReorder;

/* CmdClear is a structured data type. */
typedef struct CmdClear {
    char *op;
} CmdClear;

/* CmdPlay is a structured data type. */
typedef struct CmdPlay {
    char *op;
    uint64_t *index;
} CmdPlay;

/* CmdPause is a structured data type. */
typedef struct CmdPause {
    char *op;
} CmdPause;

/* CmdNext is a structured data type. */
typedef struct CmdNext {
    char *op;
} CmdNext;

/* CmdPrevious is a structured data type. */
typedef struct CmdPrevious {
    char *op;
} CmdPrevious;

/* CmdSeek is a structured data type. */
typedef struct CmdSeek {
    char *op;
    uint64_t position_ms;
} CmdSeek;

/* CmdVolume is a structured data type. */
typedef struct CmdVolume {
    char *op;
    uint64_t volume;
} CmdVolume;

/* PlayerCommand is a tagged union. */
typedef enum PlayerCommandTag {
    PLAYER_COMMAND_CMD_ENQUEUE,
    PLAYER_COMMAND_CMD_REMOVE,
    PLAYER_COMMAND_CMD_REORDER,
    PLAYER_COMMAND_CMD_CLEAR,
    PLAYER_COMMAND_CMD_PLAY,
    PLAYER_COMMAND_CMD_PAUSE,
    PLAYER_COMMAND_CMD_NEXT,
    PLAYER_COMMAND_CMD_PREVIOUS,
    PLAYER_COMMAND_CMD_SEEK,
    PLAYER_COMMAND_CMD_VOLUME,
} PlayerCommandTag;

typedef struct PlayerCommand {
    PlayerCommandTag tag;
    union {
        CmdEnqueue cmd_enqueue;
        CmdRemove cmd_remove;
        CmdReorder cmd_reorder;
        CmdClear cmd_clear;
        CmdPlay cmd_play;
        CmdPause cmd_pause;
        CmdNext cmd_next;
        CmdPrevious cmd_previous;
        CmdSeek cmd_seek;
        CmdVolume cmd_volume;
    } u;
} PlayerCommand;

/* CommandRequest is a structured data type. */
typedef struct CommandRequest {
    PlayerId player_id;
    PlayerCommand command;
} CommandRequest;

/* EnableShareRequest is a structured data type. */
typedef struct EnableShareRequest {
    char *suffix;
} EnableShareRequest;

/* DisableShareRequest is a structured data type. */
typedef struct DisableShareRequest {
    PlayerId player_id;
} DisableShareRequest;

/* ShareResult is a structured data type. */
typedef struct ShareResult {
    Player player;
} ShareResult;

/* MediaOpen is a structured data type. */
typedef struct MediaOpen {
    char *kind;
    TrackId track_id;
    StreamPref pref;
} MediaOpen;

/* MediaSeek is a structured data type. */
typedef struct MediaSeek {
    char *kind;
    uint64_t position_ms;
} MediaSeek;

/* MediaPause is a structured data type. */
typedef struct MediaPause {
    char *kind;
} MediaPause;

/* MediaResume is a structured data type. */
typedef struct MediaResume {
    char *kind;
} MediaResume;

/* MediaStop is a structured data type. */
typedef struct MediaStop {
    char *kind;
} MediaStop;

/* MediaControl is a tagged union. */
typedef enum MediaControlTag {
    MEDIA_CONTROL_MEDIA_OPEN,
    MEDIA_CONTROL_MEDIA_SEEK,
    MEDIA_CONTROL_MEDIA_PAUSE,
    MEDIA_CONTROL_MEDIA_RESUME,
    MEDIA_CONTROL_MEDIA_STOP,
} MediaControlTag;

typedef struct MediaControl {
    MediaControlTag tag;
    union {
        MediaOpen media_open;
        MediaSeek media_seek;
        MediaPause media_pause;
        MediaResume media_resume;
        MediaStop media_stop;
    } u;
} MediaControl;

/* MediaHeader is a structured data type. */
typedef struct MediaHeader {
    char *kind;
    Codec codec;
    bool transcoded;
    uint64_t sample_rate;
    uint64_t channels;
    uint64_t *duration_ms;
    uint64_t trim_start_samples;
    uint64_t trim_end_samples;
    CsilBytes *codec_config;
} MediaHeader;

/* MediaChunk is a structured data type. */
typedef struct MediaChunk {
    char *kind;
    uint64_t seq;
    uint64_t *timestamp_ms;
    CsilBytes data;
} MediaChunk;

/* MediaEnd is a structured data type. */
typedef struct MediaEnd {
    char *kind;
    MediaEndReason *reason;
} MediaEnd;

/* MediaFail is a structured data type. */
typedef struct MediaFail {
    char *kind;
    ServiceError error;
} MediaFail;

/* MediaEvent is a tagged union. */
typedef enum MediaEventTag {
    MEDIA_EVENT_MEDIA_HEADER,
    MEDIA_EVENT_MEDIA_CHUNK,
    MEDIA_EVENT_MEDIA_END,
    MEDIA_EVENT_MEDIA_FAIL,
} MediaEventTag;

typedef struct MediaEvent {
    MediaEventTag tag;
    union {
        MediaHeader media_header;
        MediaChunk media_chunk;
        MediaEnd media_end;
        MediaFail media_fail;
    } u;
} MediaEvent;

/* AudioOutput is a structured data type. */
typedef struct AudioOutput {
    DeviceId os_device_id;
    char *friendly_name;
    uint64_t channels;
    uint64_t *sample_rates;
    size_t sample_rates_count;
    bool is_default;
} AudioOutput;

/* RegisterNodeRequest is a structured data type. */
typedef struct RegisterNodeRequest {
    char *hostname;
    char *platform;
    char *arch;
    AudioOutput *outputs;
    size_t outputs_count;
} RegisterNodeRequest;

/* RegisterNodeResponse is a structured data type. */
typedef struct RegisterNodeResponse {
    NodeId node_id;
    Player *players;
    size_t players_count;
} RegisterNodeResponse;

/* DirLoad is a structured data type. */
typedef struct DirLoad {
    char *op;
    PlayerId player_id;
    TrackId track_id;
    StreamPref pref;
    uint64_t *position_ms;
} DirLoad;

/* DirPause is a structured data type. */
typedef struct DirPause {
    char *op;
    PlayerId player_id;
} DirPause;

/* DirResume is a structured data type. */
typedef struct DirResume {
    char *op;
    PlayerId player_id;
} DirResume;

/* DirStop is a structured data type. */
typedef struct DirStop {
    char *op;
    PlayerId player_id;
} DirStop;

/* DirVolume is a structured data type. */
typedef struct DirVolume {
    char *op;
    PlayerId player_id;
    uint64_t volume;
} DirVolume;

/* NodeDirective is a tagged union. */
typedef enum NodeDirectiveTag {
    NODE_DIRECTIVE_DIR_LOAD,
    NODE_DIRECTIVE_DIR_PAUSE,
    NODE_DIRECTIVE_DIR_RESUME,
    NODE_DIRECTIVE_DIR_STOP,
    NODE_DIRECTIVE_DIR_VOLUME,
} NodeDirectiveTag;

typedef struct NodeDirective {
    NodeDirectiveTag tag;
    union {
        DirLoad dir_load;
        DirPause dir_pause;
        DirResume dir_resume;
        DirStop dir_stop;
        DirVolume dir_volume;
    } u;
} NodeDirective;

/* NodeReport is a structured data type. */
typedef struct NodeReport {
    PlayerId player_id;
    PlayerStatus status;
    uint64_t *position_ms;
} NodeReport;

/* Account is a structured data type. */
typedef struct Account {
    AccountId id;
    Handle handle;
    char *display_name;
    Role role;
    CsilTimestamp created_at;
} Account;

/* ListAccountsResponse is a structured data type. */
typedef struct ListAccountsResponse {
    Account *accounts;
    size_t accounts_count;
} ListAccountsResponse;

/* SetRoleRequest is a structured data type. */
typedef struct SetRoleRequest {
    AccountId account_id;
    Role role;
} SetRoleRequest;

/* TrustDomainRequest is a structured data type. */
typedef struct TrustDomainRequest {
    char *domain;
} TrustDomainRequest;

/* TrustedDomains is a structured data type. */
typedef struct TrustedDomains {
    char **domains;
    size_t domains_count;
} TrustedDomains;

/* DeviceInfo is a structured data type. */
typedef struct DeviceInfo {
    DeviceId id;
    char *os_device_id;
    char *friendly_name;
    bool is_default;
} DeviceInfo;

/* NodeInfo is a structured data type. */
typedef struct NodeInfo {
    NodeId id;
    NodeKind kind;
    char *hostname;
    char *friendly_name;
    char *platform;
    char *arch;
    CsilTimestamp *last_seen;
    AudioOutputsState audio_outputs;
    DeviceInfo *devices;
    size_t devices_count;
} NodeInfo;

/* ListNodesResponse is a structured data type. */
typedef struct ListNodesResponse {
    NodeInfo *nodes;
    size_t nodes_count;
} ListNodesResponse;

/* RenameNodeRequest is a structured data type. */
typedef struct RenameNodeRequest {
    NodeId node_id;
    char *friendly_name;
} RenameNodeRequest;

/* RenameDeviceRequest is a structured data type. */
typedef struct RenameDeviceRequest {
    DeviceId device_id;
    char *friendly_name;
} RenameDeviceRequest;

/* CreateNodeTokenRequest is a structured data type. */
typedef struct CreateNodeTokenRequest {
    char *label;
} CreateNodeTokenRequest;

/* NodeTokenResult is a structured data type. */
typedef struct NodeTokenResult {
    char *token;
    char **fingerprints;
    size_t fingerprints_count;
} NodeTokenResult;

/* ImportTrackRequest is a structured data type. */
typedef struct ImportTrackRequest {
    char *root_relative_path;
    char *content_type;
    char *content_hash;
    CsilBytes data;
} ImportTrackRequest;

/* ImportResult is a structured data type. */
typedef struct ImportResult {
    bool imported;
    TrackId *track_id;
    bool skipped_existing;
} ImportResult;

/* Settings is a structured data type. */
typedef struct Settings {
    char **entries_keys;
    char **entries_values;
    size_t entries_count;
} Settings;

/* SetSettingRequest is a structured data type. */
typedef struct SetSettingRequest {
    char *key;
    char *value;
} SetSettingRequest;

#endif /* CSILGEN_TYPES_GEN_H */
