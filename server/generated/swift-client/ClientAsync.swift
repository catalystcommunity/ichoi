// Generated CSIL service clients.
// Code generated by csilgen; DO NOT EDIT.

/// The caller-supplied byte carrier: it performs the call named by (service, op)
/// with the already-encoded request bytes and returns the response bytes, or
/// throws. Asynchronous — the seam suspends on the I/O round-trip. The generated
/// client owns (de)serialization; the carrier only moves bytes.
public protocol AsyncCsilTransport {
    func call(service: String, op: String, request: [UInt8]) async throws -> [UInt8]
}

/// SessionAsyncClient is a typed client for the SessionService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct SessionAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func authenticate(_ request: AuthRequest) async throws -> SessionInfo {
        let csilResp = try await transport.call(service: "SessionService", op: "authenticate", request: request.toCbor())
        return try SessionInfo.fromCbor(csilResp)
    }

    public func whoami(_ request: Page) async throws -> SessionInfo {
        let csilResp = try await transport.call(service: "SessionService", op: "whoami", request: request.toCbor())
        return try SessionInfo.fromCbor(csilResp)
    }

    public func logout(_ request: Page) async throws -> Ok {
        let csilResp = try await transport.call(service: "SessionService", op: "logout", request: request.toCbor())
        return try Ok.fromCbor(csilResp)
    }

}

/// LibraryAsyncClient is a typed client for the LibraryService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct LibraryAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func listLibraries(_ request: Page) async throws -> LibrariesResponse {
        let csilResp = try await transport.call(service: "LibraryService", op: "list-libraries", request: request.toCbor())
        return try LibrariesResponse.fromCbor(csilResp)
    }

    public func listAlbums(_ request: BrowseRequest) async throws -> AlbumsResponse {
        let csilResp = try await transport.call(service: "LibraryService", op: "list-albums", request: request.toCbor())
        return try AlbumsResponse.fromCbor(csilResp)
    }

    public func listArtists(_ request: BrowseRequest) async throws -> ArtistsResponse {
        let csilResp = try await transport.call(service: "LibraryService", op: "list-artists", request: request.toCbor())
        return try ArtistsResponse.fromCbor(csilResp)
    }

    public func getAlbum(_ request: AlbumRequest) async throws -> AlbumDetail {
        let csilResp = try await transport.call(service: "LibraryService", op: "get-album", request: request.toCbor())
        return try AlbumDetail.fromCbor(csilResp)
    }

    public func getArtist(_ request: ArtistRequest) async throws -> ArtistDetail {
        let csilResp = try await transport.call(service: "LibraryService", op: "get-artist", request: request.toCbor())
        return try ArtistDetail.fromCbor(csilResp)
    }

    public func search(_ request: SearchRequest) async throws -> SearchResponse {
        let csilResp = try await transport.call(service: "LibraryService", op: "search", request: request.toCbor())
        return try SearchResponse.fromCbor(csilResp)
    }

    public func listPlaylists(_ request: BrowseRequest) async throws -> PlaylistsResponse {
        let csilResp = try await transport.call(service: "LibraryService", op: "list-playlists", request: request.toCbor())
        return try PlaylistsResponse.fromCbor(csilResp)
    }

    public func getPlaylist(_ request: PlaylistRequest) async throws -> PlaylistDetail {
        let csilResp = try await transport.call(service: "LibraryService", op: "get-playlist", request: request.toCbor())
        return try PlaylistDetail.fromCbor(csilResp)
    }

    public func getCoverArt(_ request: CoverArtRequest) async throws -> CoverArt {
        let csilResp = try await transport.call(service: "LibraryService", op: "get-cover-art", request: request.toCbor())
        return try CoverArt.fromCbor(csilResp)
    }

    public func getAudiobookProgress(_ request: AudiobookProgressRequest) async throws -> AudiobookProgressResponse {
        let csilResp = try await transport.call(service: "LibraryService", op: "get-audiobook-progress", request: request.toCbor())
        return try AudiobookProgressResponse.fromCbor(csilResp)
    }

    public func updateAudiobookProgress(_ request: UpdateAudiobookProgressRequest) async throws -> AudiobookProgress {
        let csilResp = try await transport.call(service: "LibraryService", op: "update-audiobook-progress", request: request.toCbor())
        return try AudiobookProgress.fromCbor(csilResp)
    }

}

/// PlayerAsyncClient is a typed client for the PlayerService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct PlayerAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func listPlayers(_ request: ListPlayersRequest) async throws -> ListPlayersResponse {
        let csilResp = try await transport.call(service: "PlayerService", op: "list-players", request: request.toCbor())
        return try ListPlayersResponse.fromCbor(csilResp)
    }

    // channel operation 'subscribe' is not part of the RPC client
    public func control(_ request: CommandRequest) async throws -> PlayerState {
        let csilResp = try await transport.call(service: "PlayerService", op: "control", request: request.toCbor())
        return try PlayerState.fromCbor(csilResp)
    }

    public func enableShare(_ request: EnableShareRequest) async throws -> ShareResult {
        let csilResp = try await transport.call(service: "PlayerService", op: "enable-share", request: request.toCbor())
        return try ShareResult.fromCbor(csilResp)
    }

    public func disableShare(_ request: DisableShareRequest) async throws -> Ok {
        let csilResp = try await transport.call(service: "PlayerService", op: "disable-share", request: request.toCbor())
        return try Ok.fromCbor(csilResp)
    }

}

/// MediaAsyncClient is a typed client for the MediaService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct MediaAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    // channel operation 'stream' is not part of the RPC client
}

/// NodeAsyncClient is a typed client for the NodeService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct NodeAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func register(_ request: RegisterNodeRequest) async throws -> RegisterNodeResponse {
        let csilResp = try await transport.call(service: "NodeService", op: "register", request: request.toCbor())
        return try RegisterNodeResponse.fromCbor(csilResp)
    }

    // channel operation 'session' is not part of the RPC client
}

/// AdminAsyncClient is a typed client for the AdminService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct AdminAsyncClient {
    public let transport: AsyncCsilTransport
    public init(transport: AsyncCsilTransport) {
        self.transport = transport
    }

    public func listAccounts(_ request: Page) async throws -> ListAccountsResponse {
        let csilResp = try await transport.call(service: "AdminService", op: "list-accounts", request: request.toCbor())
        return try ListAccountsResponse.fromCbor(csilResp)
    }

    public func setRole(_ request: SetRoleRequest) async throws -> Account {
        let csilResp = try await transport.call(service: "AdminService", op: "set-role", request: request.toCbor())
        return try Account.fromCbor(csilResp)
    }

    public func trustDomain(_ request: TrustDomainRequest) async throws -> TrustedDomains {
        let csilResp = try await transport.call(service: "AdminService", op: "trust-domain", request: request.toCbor())
        return try TrustedDomains.fromCbor(csilResp)
    }

    public func listTrustedDomains(_ request: Page) async throws -> TrustedDomains {
        let csilResp = try await transport.call(service: "AdminService", op: "list-trusted-domains", request: request.toCbor())
        return try TrustedDomains.fromCbor(csilResp)
    }

    public func listNodes(_ request: Page) async throws -> ListNodesResponse {
        let csilResp = try await transport.call(service: "AdminService", op: "list-nodes", request: request.toCbor())
        return try ListNodesResponse.fromCbor(csilResp)
    }

    public func renameNode(_ request: RenameNodeRequest) async throws -> NodeInfo {
        let csilResp = try await transport.call(service: "AdminService", op: "rename-node", request: request.toCbor())
        return try NodeInfo.fromCbor(csilResp)
    }

    public func renameDevice(_ request: RenameDeviceRequest) async throws -> DeviceInfo {
        let csilResp = try await transport.call(service: "AdminService", op: "rename-device", request: request.toCbor())
        return try DeviceInfo.fromCbor(csilResp)
    }

    public func createNodeToken(_ request: CreateNodeTokenRequest) async throws -> NodeTokenResult {
        let csilResp = try await transport.call(service: "AdminService", op: "create-node-token", request: request.toCbor())
        return try NodeTokenResult.fromCbor(csilResp)
    }

    public func importTrack(_ request: ImportTrackRequest) async throws -> ImportResult {
        let csilResp = try await transport.call(service: "AdminService", op: "import-track", request: request.toCbor())
        return try ImportResult.fromCbor(csilResp)
    }

    public func getSettings(_ request: Page) async throws -> Settings {
        let csilResp = try await transport.call(service: "AdminService", op: "get-settings", request: request.toCbor())
        return try Settings.fromCbor(csilResp)
    }

    public func setSetting(_ request: SetSettingRequest) async throws -> Settings {
        let csilResp = try await transport.call(service: "AdminService", op: "set-setting", request: request.toCbor())
        return try Settings.fromCbor(csilResp)
    }

}

