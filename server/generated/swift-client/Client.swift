// Generated CSIL service clients.
// Code generated by csilgen; DO NOT EDIT.

/// A structured error from a generated client call: a service-returned error
/// (code/message), or a transport-level failure.
public struct CsilClientError: Error, Equatable {
    public let code: Int64
    public let message: String
    public init(code: Int64, message: String) {
        self.code = code
        self.message = message
    }
}

/// The caller-supplied byte carrier: it performs the call named by (service, op)
/// with the already-encoded request bytes and returns the response bytes, or
/// throws. Synchronous and blocking — the host owns the I/O loop. The generated
/// client owns (de)serialization; the carrier only moves bytes.
public protocol CsilTransport {
    func call(service: String, op: String, request: [UInt8]) throws -> [UInt8]
}

/// SessionClient is a typed client for the SessionService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct SessionClient {
    public let transport: CsilTransport
    public init(transport: CsilTransport) {
        self.transport = transport
    }

    public func authenticate(_ request: AuthRequest) throws -> SessionInfo {
        let csilResp = try transport.call(service: "SessionService", op: "authenticate", request: request.toCbor())
        return try SessionInfo.fromCbor(csilResp)
    }

    public func whoami(_ request: Page) throws -> SessionInfo {
        let csilResp = try transport.call(service: "SessionService", op: "whoami", request: request.toCbor())
        return try SessionInfo.fromCbor(csilResp)
    }

    public func logout(_ request: Page) throws -> Ok {
        let csilResp = try transport.call(service: "SessionService", op: "logout", request: request.toCbor())
        return try Ok.fromCbor(csilResp)
    }

}

/// LibraryClient is a typed client for the LibraryService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct LibraryClient {
    public let transport: CsilTransport
    public init(transport: CsilTransport) {
        self.transport = transport
    }

    public func listLibraries(_ request: Page) throws -> LibrariesResponse {
        let csilResp = try transport.call(service: "LibraryService", op: "list-libraries", request: request.toCbor())
        return try LibrariesResponse.fromCbor(csilResp)
    }

    public func listAlbums(_ request: BrowseRequest) throws -> AlbumsResponse {
        let csilResp = try transport.call(service: "LibraryService", op: "list-albums", request: request.toCbor())
        return try AlbumsResponse.fromCbor(csilResp)
    }

    public func listArtists(_ request: BrowseRequest) throws -> ArtistsResponse {
        let csilResp = try transport.call(service: "LibraryService", op: "list-artists", request: request.toCbor())
        return try ArtistsResponse.fromCbor(csilResp)
    }

    public func getAlbum(_ request: AlbumRequest) throws -> AlbumDetail {
        let csilResp = try transport.call(service: "LibraryService", op: "get-album", request: request.toCbor())
        return try AlbumDetail.fromCbor(csilResp)
    }

    public func getArtist(_ request: ArtistRequest) throws -> ArtistDetail {
        let csilResp = try transport.call(service: "LibraryService", op: "get-artist", request: request.toCbor())
        return try ArtistDetail.fromCbor(csilResp)
    }

    public func search(_ request: SearchRequest) throws -> SearchResponse {
        let csilResp = try transport.call(service: "LibraryService", op: "search", request: request.toCbor())
        return try SearchResponse.fromCbor(csilResp)
    }

    public func listPlaylists(_ request: BrowseRequest) throws -> PlaylistsResponse {
        let csilResp = try transport.call(service: "LibraryService", op: "list-playlists", request: request.toCbor())
        return try PlaylistsResponse.fromCbor(csilResp)
    }

    public func getPlaylist(_ request: PlaylistRequest) throws -> PlaylistDetail {
        let csilResp = try transport.call(service: "LibraryService", op: "get-playlist", request: request.toCbor())
        return try PlaylistDetail.fromCbor(csilResp)
    }

    public func getCoverArt(_ request: CoverArtRequest) throws -> CoverArt {
        let csilResp = try transport.call(service: "LibraryService", op: "get-cover-art", request: request.toCbor())
        return try CoverArt.fromCbor(csilResp)
    }

    public func getAudiobookProgress(_ request: AudiobookProgressRequest) throws -> AudiobookProgressResponse {
        let csilResp = try transport.call(service: "LibraryService", op: "get-audiobook-progress", request: request.toCbor())
        return try AudiobookProgressResponse.fromCbor(csilResp)
    }

    public func updateAudiobookProgress(_ request: UpdateAudiobookProgressRequest) throws -> AudiobookProgress {
        let csilResp = try transport.call(service: "LibraryService", op: "update-audiobook-progress", request: request.toCbor())
        return try AudiobookProgress.fromCbor(csilResp)
    }

}

/// PlayerClient is a typed client for the PlayerService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct PlayerClient {
    public let transport: CsilTransport
    public init(transport: CsilTransport) {
        self.transport = transport
    }

    public func listPlayers(_ request: ListPlayersRequest) throws -> ListPlayersResponse {
        let csilResp = try transport.call(service: "PlayerService", op: "list-players", request: request.toCbor())
        return try ListPlayersResponse.fromCbor(csilResp)
    }

    // channel operation 'subscribe' is not part of the RPC client
    public func control(_ request: CommandRequest) throws -> PlayerState {
        let csilResp = try transport.call(service: "PlayerService", op: "control", request: request.toCbor())
        return try PlayerState.fromCbor(csilResp)
    }

    public func enableShare(_ request: EnableShareRequest) throws -> ShareResult {
        let csilResp = try transport.call(service: "PlayerService", op: "enable-share", request: request.toCbor())
        return try ShareResult.fromCbor(csilResp)
    }

    public func disableShare(_ request: DisableShareRequest) throws -> Ok {
        let csilResp = try transport.call(service: "PlayerService", op: "disable-share", request: request.toCbor())
        return try Ok.fromCbor(csilResp)
    }

}

/// MediaClient is a typed client for the MediaService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct MediaClient {
    public let transport: CsilTransport
    public init(transport: CsilTransport) {
        self.transport = transport
    }

    // channel operation 'stream' is not part of the RPC client
}

/// NodeClient is a typed client for the NodeService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct NodeClient {
    public let transport: CsilTransport
    public init(transport: CsilTransport) {
        self.transport = transport
    }

    public func register(_ request: RegisterNodeRequest) throws -> RegisterNodeResponse {
        let csilResp = try transport.call(service: "NodeService", op: "register", request: request.toCbor())
        return try RegisterNodeResponse.fromCbor(csilResp)
    }

    // channel operation 'session' is not part of the RPC client
}

/// AdminClient is a typed client for the AdminService service. The client owns
/// (de)serialization; the carrier only moves bytes.
public struct AdminClient {
    public let transport: CsilTransport
    public init(transport: CsilTransport) {
        self.transport = transport
    }

    public func listAccounts(_ request: Page) throws -> ListAccountsResponse {
        let csilResp = try transport.call(service: "AdminService", op: "list-accounts", request: request.toCbor())
        return try ListAccountsResponse.fromCbor(csilResp)
    }

    public func setRole(_ request: SetRoleRequest) throws -> Account {
        let csilResp = try transport.call(service: "AdminService", op: "set-role", request: request.toCbor())
        return try Account.fromCbor(csilResp)
    }

    public func trustDomain(_ request: TrustDomainRequest) throws -> TrustedDomains {
        let csilResp = try transport.call(service: "AdminService", op: "trust-domain", request: request.toCbor())
        return try TrustedDomains.fromCbor(csilResp)
    }

    public func listTrustedDomains(_ request: Page) throws -> TrustedDomains {
        let csilResp = try transport.call(service: "AdminService", op: "list-trusted-domains", request: request.toCbor())
        return try TrustedDomains.fromCbor(csilResp)
    }

    public func listNodes(_ request: Page) throws -> ListNodesResponse {
        let csilResp = try transport.call(service: "AdminService", op: "list-nodes", request: request.toCbor())
        return try ListNodesResponse.fromCbor(csilResp)
    }

    public func renameNode(_ request: RenameNodeRequest) throws -> NodeInfo {
        let csilResp = try transport.call(service: "AdminService", op: "rename-node", request: request.toCbor())
        return try NodeInfo.fromCbor(csilResp)
    }

    public func renameDevice(_ request: RenameDeviceRequest) throws -> DeviceInfo {
        let csilResp = try transport.call(service: "AdminService", op: "rename-device", request: request.toCbor())
        return try DeviceInfo.fromCbor(csilResp)
    }

    public func setDeviceAccess(_ request: SetDeviceAccessRequest) throws -> DeviceInfo {
        let csilResp = try transport.call(service: "AdminService", op: "set-device-access", request: request.toCbor())
        return try DeviceInfo.fromCbor(csilResp)
    }

    public func listGroups(_ request: Page) throws -> ListGroupsResponse {
        let csilResp = try transport.call(service: "AdminService", op: "list-groups", request: request.toCbor())
        return try ListGroupsResponse.fromCbor(csilResp)
    }

    public func createGroup(_ request: CreateGroupRequest) throws -> GroupInfo {
        let csilResp = try transport.call(service: "AdminService", op: "create-group", request: request.toCbor())
        return try GroupInfo.fromCbor(csilResp)
    }

    public func setGroupMembers(_ request: SetGroupMembersRequest) throws -> GroupInfo {
        let csilResp = try transport.call(service: "AdminService", op: "set-group-members", request: request.toCbor())
        return try GroupInfo.fromCbor(csilResp)
    }

    public func deleteGroup(_ request: DeleteGroupRequest) throws -> Ok {
        let csilResp = try transport.call(service: "AdminService", op: "delete-group", request: request.toCbor())
        return try Ok.fromCbor(csilResp)
    }

    public func listSatelliteTokens(_ request: Page) throws -> ListSatelliteTokensResponse {
        let csilResp = try transport.call(service: "AdminService", op: "list-satellite-tokens", request: request.toCbor())
        return try ListSatelliteTokensResponse.fromCbor(csilResp)
    }

    public func createNodeToken(_ request: CreateNodeTokenRequest) throws -> NodeTokenResult {
        let csilResp = try transport.call(service: "AdminService", op: "create-node-token", request: request.toCbor())
        return try NodeTokenResult.fromCbor(csilResp)
    }

    public func revokeSatelliteToken(_ request: RevokeSatelliteTokenRequest) throws -> Ok {
        let csilResp = try transport.call(service: "AdminService", op: "revoke-satellite-token", request: request.toCbor())
        return try Ok.fromCbor(csilResp)
    }

    public func importTrack(_ request: ImportTrackRequest) throws -> ImportResult {
        let csilResp = try transport.call(service: "AdminService", op: "import-track", request: request.toCbor())
        return try ImportResult.fromCbor(csilResp)
    }

    public func getSettings(_ request: Page) throws -> Settings {
        let csilResp = try transport.call(service: "AdminService", op: "get-settings", request: request.toCbor())
        return try Settings.fromCbor(csilResp)
    }

    public func setSetting(_ request: SetSettingRequest) throws -> Settings {
        let csilResp = try transport.call(service: "AdminService", op: "set-setting", request: request.toCbor())
        return try Settings.fromCbor(csilResp)
    }

    public func resyncLibrary(_ request: Page) throws -> LibraryResyncStatus {
        let csilResp = try transport.call(service: "AdminService", op: "resync-library", request: request.toCbor())
        return try LibraryResyncStatus.fromCbor(csilResp)
    }

    public func getResyncStatus(_ request: Page) throws -> LibraryResyncStatus {
        let csilResp = try transport.call(service: "AdminService", op: "get-resync-status", request: request.toCbor())
        return try LibraryResyncStatus.fromCbor(csilResp)
    }

}

