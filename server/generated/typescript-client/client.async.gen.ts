// Code generated by csilgen. DO NOT EDIT.
// Source: <csil spec>
// Target: typescript-client

import type { Account, AlbumDetail, AlbumRequest, AlbumsResponse, ArtistDetail, ArtistRequest, ArtistsResponse, AuthRequest, BrowseRequest, CommandRequest, CoverArt, CoverArtRequest, CreateNodeTokenRequest, DeviceInfo, DisableShareRequest, EnableShareRequest, ImportResult, ImportTrackRequest, ListAccountsResponse, ListNodesResponse, ListPlayersRequest, ListPlayersResponse, MediaControl, MediaEvent, NodeDirective, NodeInfo, NodeReport, NodeTokenResult, Ok, Page, PlayerState, PlaylistDetail, PlaylistRequest, PlaylistsResponse, RegisterNodeRequest, RegisterNodeResponse, RenameDeviceRequest, RenameNodeRequest, SearchRequest, SearchResponse, ServiceError, SessionInfo, SetRoleRequest, SetSettingRequest, Settings, ShareResult, SubscribeRequest, TrustDomainRequest, TrustedDomains } from "./types.gen.ts";
import { fromAccountCbor, fromAlbumDetailCbor, fromAlbumsResponseCbor, fromArtistDetailCbor, fromArtistsResponseCbor, fromCoverArtCbor, fromDeviceInfoCbor, fromImportResultCbor, fromListAccountsResponseCbor, fromListNodesResponseCbor, fromListPlayersResponseCbor, fromNodeInfoCbor, fromNodeTokenResultCbor, fromOkCbor, fromPlayerStateCbor, fromPlaylistDetailCbor, fromPlaylistsResponseCbor, fromRegisterNodeResponseCbor, fromSearchResponseCbor, fromSessionInfoCbor, fromSettingsCbor, fromShareResultCbor, fromTrustedDomainsCbor, toAlbumRequestCbor, toArtistRequestCbor, toAuthRequestCbor, toBrowseRequestCbor, toCommandRequestCbor, toCoverArtRequestCbor, toCreateNodeTokenRequestCbor, toDisableShareRequestCbor, toEnableShareRequestCbor, toImportTrackRequestCbor, toListPlayersRequestCbor, toPageCbor, toPlaylistRequestCbor, toRegisterNodeRequestCbor, toRenameDeviceRequestCbor, toRenameNodeRequestCbor, toSearchRequestCbor, toSetRoleRequestCbor, toSetSettingRequestCbor, toTrustDomainRequestCbor } from "./codec.gen.ts";

export interface AsyncServiceTransport {
  call(service: string, op: string, req: Uint8Array): Promise<Uint8Array>;
}

export interface AsyncCodec {
  decode<T>(bytes: Uint8Array): T;
  encode(value: unknown): Uint8Array;
}

export class AdminAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listAccounts(req: Page): Promise<ListAccountsResponse> {
    const csilResp = await this.t.call("AdminService", "list-accounts", toPageCbor(req));
    return fromListAccountsResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async setRole(req: SetRoleRequest): Promise<Account> {
    const csilResp = await this.t.call("AdminService", "set-role", toSetRoleRequestCbor(req));
    return fromAccountCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async trustDomain(req: TrustDomainRequest): Promise<TrustedDomains> {
    const csilResp = await this.t.call("AdminService", "trust-domain", toTrustDomainRequestCbor(req));
    return fromTrustedDomainsCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listTrustedDomains(req: Page): Promise<TrustedDomains> {
    const csilResp = await this.t.call("AdminService", "list-trusted-domains", toPageCbor(req));
    return fromTrustedDomainsCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listNodes(req: Page): Promise<ListNodesResponse> {
    const csilResp = await this.t.call("AdminService", "list-nodes", toPageCbor(req));
    return fromListNodesResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async renameNode(req: RenameNodeRequest): Promise<NodeInfo> {
    const csilResp = await this.t.call("AdminService", "rename-node", toRenameNodeRequestCbor(req));
    return fromNodeInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async renameDevice(req: RenameDeviceRequest): Promise<DeviceInfo> {
    const csilResp = await this.t.call("AdminService", "rename-device", toRenameDeviceRequestCbor(req));
    return fromDeviceInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async createNodeToken(req: CreateNodeTokenRequest): Promise<NodeTokenResult> {
    const csilResp = await this.t.call("AdminService", "create-node-token", toCreateNodeTokenRequestCbor(req));
    return fromNodeTokenResultCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async importTrack(req: ImportTrackRequest): Promise<ImportResult> {
    const csilResp = await this.t.call("AdminService", "import-track", toImportTrackRequestCbor(req));
    return fromImportResultCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getSettings(req: Page): Promise<Settings> {
    const csilResp = await this.t.call("AdminService", "get-settings", toPageCbor(req));
    return fromSettingsCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async setSetting(req: SetSettingRequest): Promise<Settings> {
    const csilResp = await this.t.call("AdminService", "set-setting", toSetSettingRequestCbor(req));
    return fromSettingsCbor(csilResp);
  }
}

export class LibraryAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listAlbums(req: BrowseRequest): Promise<AlbumsResponse> {
    const csilResp = await this.t.call("LibraryService", "list-albums", toBrowseRequestCbor(req));
    return fromAlbumsResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listArtists(req: BrowseRequest): Promise<ArtistsResponse> {
    const csilResp = await this.t.call("LibraryService", "list-artists", toBrowseRequestCbor(req));
    return fromArtistsResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getAlbum(req: AlbumRequest): Promise<AlbumDetail> {
    const csilResp = await this.t.call("LibraryService", "get-album", toAlbumRequestCbor(req));
    return fromAlbumDetailCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getArtist(req: ArtistRequest): Promise<ArtistDetail> {
    const csilResp = await this.t.call("LibraryService", "get-artist", toArtistRequestCbor(req));
    return fromArtistDetailCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async search(req: SearchRequest): Promise<SearchResponse> {
    const csilResp = await this.t.call("LibraryService", "search", toSearchRequestCbor(req));
    return fromSearchResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listPlaylists(req: BrowseRequest): Promise<PlaylistsResponse> {
    const csilResp = await this.t.call("LibraryService", "list-playlists", toBrowseRequestCbor(req));
    return fromPlaylistsResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getPlaylist(req: PlaylistRequest): Promise<PlaylistDetail> {
    const csilResp = await this.t.call("LibraryService", "get-playlist", toPlaylistRequestCbor(req));
    return fromPlaylistDetailCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async getCoverArt(req: CoverArtRequest): Promise<CoverArt> {
    const csilResp = await this.t.call("LibraryService", "get-cover-art", toCoverArtRequestCbor(req));
    return fromCoverArtCbor(csilResp);
  }
}

export interface MediaAsyncChannelHandlers {
  stream(msg: MediaEvent): void | Promise<void>;
}

/**
 * Dispatch one inbound frame for the MediaService channel. The implementer
 * (WebSocket adapter etc.) calls this for each message it pulls off the wire;
 * this generator never owns the connection itself.
 */
export async function routeMediaAsyncChannel(
  handlers: MediaAsyncChannelHandlers,
  codec: AsyncCodec,
  method: string,
  bytes: Uint8Array,
): Promise<void> {
  switch (method) {
    case "stream":
      await handlers.stream(codec.decode<MediaEvent>(bytes));
      return;
    default:
      throw { code: 404, message: `unknown channel ${method}` } satisfies ServiceError;
  }
}

/**
 * Encode an outbound `stream` message; hand the resulting bytes to
 * your connection. Returns `{method, bytes}` so the implementer can frame
 * both pieces however its protocol requires.
 */
export function encodeMediaAsyncStream(codec: AsyncCodec, msg: MediaControl): { method: string; bytes: Uint8Array } {
  return { method: "stream", bytes: codec.encode(msg) };
}

export class NodeAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async register(req: RegisterNodeRequest): Promise<RegisterNodeResponse> {
    const csilResp = await this.t.call("NodeService", "register", toRegisterNodeRequestCbor(req));
    return fromRegisterNodeResponseCbor(csilResp);
  }
}

export interface NodeAsyncChannelHandlers {
  session(msg: NodeDirective): void | Promise<void>;
}

/**
 * Dispatch one inbound frame for the NodeService channel. The implementer
 * (WebSocket adapter etc.) calls this for each message it pulls off the wire;
 * this generator never owns the connection itself.
 */
export async function routeNodeAsyncChannel(
  handlers: NodeAsyncChannelHandlers,
  codec: AsyncCodec,
  method: string,
  bytes: Uint8Array,
): Promise<void> {
  switch (method) {
    case "session":
      await handlers.session(codec.decode<NodeDirective>(bytes));
      return;
    default:
      throw { code: 404, message: `unknown channel ${method}` } satisfies ServiceError;
  }
}

/**
 * Encode an outbound `session` message; hand the resulting bytes to
 * your connection. Returns `{method, bytes}` so the implementer can frame
 * both pieces however its protocol requires.
 */
export function encodeNodeAsyncSession(codec: AsyncCodec, msg: NodeReport): { method: string; bytes: Uint8Array } {
  return { method: "session", bytes: codec.encode(msg) };
}

export class PlayerAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async listPlayers(req: ListPlayersRequest): Promise<ListPlayersResponse> {
    const csilResp = await this.t.call("PlayerService", "list-players", toListPlayersRequestCbor(req));
    return fromListPlayersResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async control(req: CommandRequest): Promise<PlayerState> {
    const csilResp = await this.t.call("PlayerService", "control", toCommandRequestCbor(req));
    return fromPlayerStateCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async enableShare(req: EnableShareRequest): Promise<ShareResult> {
    const csilResp = await this.t.call("PlayerService", "enable-share", toEnableShareRequestCbor(req));
    return fromShareResultCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async disableShare(req: DisableShareRequest): Promise<Ok> {
    const csilResp = await this.t.call("PlayerService", "disable-share", toDisableShareRequestCbor(req));
    return fromOkCbor(csilResp);
  }
}

export interface PlayerAsyncChannelHandlers {
  subscribe(msg: PlayerState): void | Promise<void>;
}

/**
 * Dispatch one inbound frame for the PlayerService channel. The implementer
 * (WebSocket adapter etc.) calls this for each message it pulls off the wire;
 * this generator never owns the connection itself.
 */
export async function routePlayerAsyncChannel(
  handlers: PlayerAsyncChannelHandlers,
  codec: AsyncCodec,
  method: string,
  bytes: Uint8Array,
): Promise<void> {
  switch (method) {
    case "subscribe":
      await handlers.subscribe(codec.decode<PlayerState>(bytes));
      return;
    default:
      throw { code: 404, message: `unknown channel ${method}` } satisfies ServiceError;
  }
}

/**
 * Encode an outbound `subscribe` message; hand the resulting bytes to
 * your connection. Returns `{method, bytes}` so the implementer can frame
 * both pieces however its protocol requires.
 */
export function encodePlayerAsyncSubscribe(codec: AsyncCodec, msg: SubscribeRequest): { method: string; bytes: Uint8Array } {
  return { method: "subscribe", bytes: codec.encode(msg) };
}

export class SessionAsyncClient {
  constructor(private readonly t: AsyncServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async authenticate(req: AuthRequest): Promise<SessionInfo> {
    const csilResp = await this.t.call("SessionService", "authenticate", toAuthRequestCbor(req));
    return fromSessionInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async whoami(req: Page): Promise<SessionInfo> {
    const csilResp = await this.t.call("SessionService", "whoami", toPageCbor(req));
    return fromSessionInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  async logout(req: Page): Promise<Ok> {
    const csilResp = await this.t.call("SessionService", "logout", toPageCbor(req));
    return fromOkCbor(csilResp);
  }
}

export class AsyncApiClient {
  readonly admin: AdminAsyncClient;
  readonly library: LibraryAsyncClient;
  readonly node: NodeAsyncClient;
  readonly player: PlayerAsyncClient;
  readonly session: SessionAsyncClient;
  constructor(t: AsyncServiceTransport) {
    this.admin = new AdminAsyncClient(t);
    this.library = new LibraryAsyncClient(t);
    this.node = new NodeAsyncClient(t);
    this.player = new PlayerAsyncClient(t);
    this.session = new SessionAsyncClient(t);
  }
}
