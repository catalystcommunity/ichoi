// Code generated by csilgen. DO NOT EDIT.
// Source: <csil spec>
// Target: typescript-client

import type { Account, AlbumDetail, AlbumRequest, AlbumsResponse, ArtistDetail, ArtistRequest, ArtistsResponse, AudiobookProgress, AudiobookProgressRequest, AudiobookProgressResponse, AuthRequest, BrowseRequest, CommandRequest, CoverArt, CoverArtRequest, CreateGroupRequest, CreateNodeTokenRequest, DeleteGroupRequest, DeviceInfo, DisableShareRequest, EnableShareRequest, GroupInfo, ImportResult, ImportTrackRequest, LibrariesResponse, LibraryResyncStatus, ListAccountsResponse, ListGroupsResponse, ListNodesResponse, ListPlayersRequest, ListPlayersResponse, ListSatelliteTokensResponse, MediaControl, MediaEvent, NodeDirective, NodeInfo, NodeReport, NodeTokenResult, Ok, Page, PlayerState, PlaylistDetail, PlaylistRequest, PlaylistsResponse, RegisterNodeRequest, RegisterNodeResponse, RenameDeviceRequest, RenameNodeRequest, RevokeSatelliteTokenRequest, SearchRequest, SearchResponse, ServiceError, SessionInfo, SetDeviceAccessRequest, SetGroupMembersRequest, SetRoleRequest, SetSettingRequest, Settings, ShareResult, SubscribeRequest, TrustDomainRequest, TrustedDomains, UpdateAudiobookProgressRequest } from "./types.gen.ts";
import { fromAccountCbor, fromAlbumDetailCbor, fromAlbumsResponseCbor, fromArtistDetailCbor, fromArtistsResponseCbor, fromAudiobookProgressCbor, fromAudiobookProgressResponseCbor, fromCoverArtCbor, fromDeviceInfoCbor, fromGroupInfoCbor, fromImportResultCbor, fromLibrariesResponseCbor, fromLibraryResyncStatusCbor, fromListAccountsResponseCbor, fromListGroupsResponseCbor, fromListNodesResponseCbor, fromListPlayersResponseCbor, fromListSatelliteTokensResponseCbor, fromNodeInfoCbor, fromNodeTokenResultCbor, fromOkCbor, fromPlayerStateCbor, fromPlaylistDetailCbor, fromPlaylistsResponseCbor, fromRegisterNodeResponseCbor, fromSearchResponseCbor, fromSessionInfoCbor, fromSettingsCbor, fromShareResultCbor, fromTrustedDomainsCbor, toAlbumRequestCbor, toArtistRequestCbor, toAudiobookProgressRequestCbor, toAuthRequestCbor, toBrowseRequestCbor, toCommandRequestCbor, toCoverArtRequestCbor, toCreateGroupRequestCbor, toCreateNodeTokenRequestCbor, toDeleteGroupRequestCbor, toDisableShareRequestCbor, toEnableShareRequestCbor, toImportTrackRequestCbor, toListPlayersRequestCbor, toPageCbor, toPlaylistRequestCbor, toRegisterNodeRequestCbor, toRenameDeviceRequestCbor, toRenameNodeRequestCbor, toRevokeSatelliteTokenRequestCbor, toSearchRequestCbor, toSetDeviceAccessRequestCbor, toSetGroupMembersRequestCbor, toSetRoleRequestCbor, toSetSettingRequestCbor, toTrustDomainRequestCbor, toUpdateAudiobookProgressRequestCbor } from "./codec.gen.ts";

export interface ServiceTransport {
  call(service: string, op: string, req: Uint8Array): Uint8Array;
}

export interface Codec {
  decode<T>(bytes: Uint8Array): T;
  encode(value: unknown): Uint8Array;
}

export class AdminClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listAccounts(req: Page): ListAccountsResponse {
    const csilResp = this.t.call("AdminService", "list-accounts", toPageCbor(req));
    return fromListAccountsResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  setRole(req: SetRoleRequest): Account {
    const csilResp = this.t.call("AdminService", "set-role", toSetRoleRequestCbor(req));
    return fromAccountCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  trustDomain(req: TrustDomainRequest): TrustedDomains {
    const csilResp = this.t.call("AdminService", "trust-domain", toTrustDomainRequestCbor(req));
    return fromTrustedDomainsCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listTrustedDomains(req: Page): TrustedDomains {
    const csilResp = this.t.call("AdminService", "list-trusted-domains", toPageCbor(req));
    return fromTrustedDomainsCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listNodes(req: Page): ListNodesResponse {
    const csilResp = this.t.call("AdminService", "list-nodes", toPageCbor(req));
    return fromListNodesResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  renameNode(req: RenameNodeRequest): NodeInfo {
    const csilResp = this.t.call("AdminService", "rename-node", toRenameNodeRequestCbor(req));
    return fromNodeInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  renameDevice(req: RenameDeviceRequest): DeviceInfo {
    const csilResp = this.t.call("AdminService", "rename-device", toRenameDeviceRequestCbor(req));
    return fromDeviceInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  setDeviceAccess(req: SetDeviceAccessRequest): DeviceInfo {
    const csilResp = this.t.call("AdminService", "set-device-access", toSetDeviceAccessRequestCbor(req));
    return fromDeviceInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listGroups(req: Page): ListGroupsResponse {
    const csilResp = this.t.call("AdminService", "list-groups", toPageCbor(req));
    return fromListGroupsResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  createGroup(req: CreateGroupRequest): GroupInfo {
    const csilResp = this.t.call("AdminService", "create-group", toCreateGroupRequestCbor(req));
    return fromGroupInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  setGroupMembers(req: SetGroupMembersRequest): GroupInfo {
    const csilResp = this.t.call("AdminService", "set-group-members", toSetGroupMembersRequestCbor(req));
    return fromGroupInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  deleteGroup(req: DeleteGroupRequest): Ok {
    const csilResp = this.t.call("AdminService", "delete-group", toDeleteGroupRequestCbor(req));
    return fromOkCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listSatelliteTokens(req: Page): ListSatelliteTokensResponse {
    const csilResp = this.t.call("AdminService", "list-satellite-tokens", toPageCbor(req));
    return fromListSatelliteTokensResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  createNodeToken(req: CreateNodeTokenRequest): NodeTokenResult {
    const csilResp = this.t.call("AdminService", "create-node-token", toCreateNodeTokenRequestCbor(req));
    return fromNodeTokenResultCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  revokeSatelliteToken(req: RevokeSatelliteTokenRequest): Ok {
    const csilResp = this.t.call("AdminService", "revoke-satellite-token", toRevokeSatelliteTokenRequestCbor(req));
    return fromOkCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  importTrack(req: ImportTrackRequest): ImportResult {
    const csilResp = this.t.call("AdminService", "import-track", toImportTrackRequestCbor(req));
    return fromImportResultCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  getSettings(req: Page): Settings {
    const csilResp = this.t.call("AdminService", "get-settings", toPageCbor(req));
    return fromSettingsCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  setSetting(req: SetSettingRequest): Settings {
    const csilResp = this.t.call("AdminService", "set-setting", toSetSettingRequestCbor(req));
    return fromSettingsCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  resyncLibrary(req: Page): LibraryResyncStatus {
    const csilResp = this.t.call("AdminService", "resync-library", toPageCbor(req));
    return fromLibraryResyncStatusCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  getResyncStatus(req: Page): LibraryResyncStatus {
    const csilResp = this.t.call("AdminService", "get-resync-status", toPageCbor(req));
    return fromLibraryResyncStatusCbor(csilResp);
  }
}

export class LibraryClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listLibraries(req: Page): LibrariesResponse {
    const csilResp = this.t.call("LibraryService", "list-libraries", toPageCbor(req));
    return fromLibrariesResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listAlbums(req: BrowseRequest): AlbumsResponse {
    const csilResp = this.t.call("LibraryService", "list-albums", toBrowseRequestCbor(req));
    return fromAlbumsResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listArtists(req: BrowseRequest): ArtistsResponse {
    const csilResp = this.t.call("LibraryService", "list-artists", toBrowseRequestCbor(req));
    return fromArtistsResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  getAlbum(req: AlbumRequest): AlbumDetail {
    const csilResp = this.t.call("LibraryService", "get-album", toAlbumRequestCbor(req));
    return fromAlbumDetailCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  getArtist(req: ArtistRequest): ArtistDetail {
    const csilResp = this.t.call("LibraryService", "get-artist", toArtistRequestCbor(req));
    return fromArtistDetailCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  search(req: SearchRequest): SearchResponse {
    const csilResp = this.t.call("LibraryService", "search", toSearchRequestCbor(req));
    return fromSearchResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listPlaylists(req: BrowseRequest): PlaylistsResponse {
    const csilResp = this.t.call("LibraryService", "list-playlists", toBrowseRequestCbor(req));
    return fromPlaylistsResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  getPlaylist(req: PlaylistRequest): PlaylistDetail {
    const csilResp = this.t.call("LibraryService", "get-playlist", toPlaylistRequestCbor(req));
    return fromPlaylistDetailCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  getCoverArt(req: CoverArtRequest): CoverArt {
    const csilResp = this.t.call("LibraryService", "get-cover-art", toCoverArtRequestCbor(req));
    return fromCoverArtCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  getAudiobookProgress(req: AudiobookProgressRequest): AudiobookProgressResponse {
    const csilResp = this.t.call("LibraryService", "get-audiobook-progress", toAudiobookProgressRequestCbor(req));
    return fromAudiobookProgressResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  updateAudiobookProgress(req: UpdateAudiobookProgressRequest): AudiobookProgress {
    const csilResp = this.t.call("LibraryService", "update-audiobook-progress", toUpdateAudiobookProgressRequestCbor(req));
    return fromAudiobookProgressCbor(csilResp);
  }
}

export interface MediaChannelHandlers {
  stream(msg: MediaEvent): void;
}

/**
 * Dispatch one inbound frame for the MediaService channel. The implementer
 * (WebSocket adapter etc.) calls this for each message it pulls off the wire;
 * this generator never owns the connection itself.
 */
export function routeMediaChannel(
  handlers: MediaChannelHandlers,
  codec: Codec,
  method: string,
  bytes: Uint8Array,
): void {
  switch (method) {
    case "stream":
      handlers.stream(codec.decode<MediaEvent>(bytes));
      return;
    default:
      throw { code: 404, message: `unknown channel ${method}` } satisfies ServiceError;
  }
}

/**
 * Encode an outbound `stream` message; hand the resulting bytes to
 * your connection. Returns `{method, bytes}` so the implementer can frame
 * both pieces however its protocol requires.
 */
export function encodeMediaStream(codec: Codec, msg: MediaControl): { method: string; bytes: Uint8Array } {
  return { method: "stream", bytes: codec.encode(msg) };
}

export class NodeClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  register(req: RegisterNodeRequest): RegisterNodeResponse {
    const csilResp = this.t.call("NodeService", "register", toRegisterNodeRequestCbor(req));
    return fromRegisterNodeResponseCbor(csilResp);
  }
}

export interface NodeChannelHandlers {
  session(msg: NodeDirective): void;
}

/**
 * Dispatch one inbound frame for the NodeService channel. The implementer
 * (WebSocket adapter etc.) calls this for each message it pulls off the wire;
 * this generator never owns the connection itself.
 */
export function routeNodeChannel(
  handlers: NodeChannelHandlers,
  codec: Codec,
  method: string,
  bytes: Uint8Array,
): void {
  switch (method) {
    case "session":
      handlers.session(codec.decode<NodeDirective>(bytes));
      return;
    default:
      throw { code: 404, message: `unknown channel ${method}` } satisfies ServiceError;
  }
}

/**
 * Encode an outbound `session` message; hand the resulting bytes to
 * your connection. Returns `{method, bytes}` so the implementer can frame
 * both pieces however its protocol requires.
 */
export function encodeNodeSession(codec: Codec, msg: NodeReport): { method: string; bytes: Uint8Array } {
  return { method: "session", bytes: codec.encode(msg) };
}

export class PlayerClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  listPlayers(req: ListPlayersRequest): ListPlayersResponse {
    const csilResp = this.t.call("PlayerService", "list-players", toListPlayersRequestCbor(req));
    return fromListPlayersResponseCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  control(req: CommandRequest): PlayerState {
    const csilResp = this.t.call("PlayerService", "control", toCommandRequestCbor(req));
    return fromPlayerStateCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  enableShare(req: EnableShareRequest): ShareResult {
    const csilResp = this.t.call("PlayerService", "enable-share", toEnableShareRequestCbor(req));
    return fromShareResultCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  disableShare(req: DisableShareRequest): Ok {
    const csilResp = this.t.call("PlayerService", "disable-share", toDisableShareRequestCbor(req));
    return fromOkCbor(csilResp);
  }
}

export interface PlayerChannelHandlers {
  subscribe(msg: PlayerState): void;
}

/**
 * Dispatch one inbound frame for the PlayerService channel. The implementer
 * (WebSocket adapter etc.) calls this for each message it pulls off the wire;
 * this generator never owns the connection itself.
 */
export function routePlayerChannel(
  handlers: PlayerChannelHandlers,
  codec: Codec,
  method: string,
  bytes: Uint8Array,
): void {
  switch (method) {
    case "subscribe":
      handlers.subscribe(codec.decode<PlayerState>(bytes));
      return;
    default:
      throw { code: 404, message: `unknown channel ${method}` } satisfies ServiceError;
  }
}

/**
 * Encode an outbound `subscribe` message; hand the resulting bytes to
 * your connection. Returns `{method, bytes}` so the implementer can frame
 * both pieces however its protocol requires.
 */
export function encodePlayerSubscribe(codec: Codec, msg: SubscribeRequest): { method: string; bytes: Uint8Array } {
  return { method: "subscribe", bytes: codec.encode(msg) };
}

export class SessionClient {
  constructor(private readonly t: ServiceTransport) {}

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  authenticate(req: AuthRequest): SessionInfo {
    const csilResp = this.t.call("SessionService", "authenticate", toAuthRequestCbor(req));
    return fromSessionInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  whoami(req: Page): SessionInfo {
    const csilResp = this.t.call("SessionService", "whoami", toPageCbor(req));
    return fromSessionInfoCbor(csilResp);
  }

  /**
   * @throws {ServiceError} when the API returns an error response
   * @throws transport errors (network, timeout) raised by the transport
   */
  logout(req: Page): Ok {
    const csilResp = this.t.call("SessionService", "logout", toPageCbor(req));
    return fromOkCbor(csilResp);
  }
}

export class ApiClient {
  readonly admin: AdminClient;
  readonly library: LibraryClient;
  readonly node: NodeClient;
  readonly player: PlayerClient;
  readonly session: SessionClient;
  constructor(t: ServiceTransport) {
    this.admin = new AdminClient(t);
    this.library = new LibraryClient(t);
    this.node = new NodeClient(t);
    this.player = new PlayerClient(t);
    this.session = new SessionClient(t);
  }
}
